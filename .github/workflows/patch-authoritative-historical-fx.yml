name: Patch authoritative historical FX
on:
  push:
    branches: [fix/sethiportfolio-authoritative-historical-fx]
    paths:
      - 'scripts/patch_authoritative_historical_fx.py'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/sethiportfolio-authoritative-historical-fx
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/patch_authoritative_historical_fx.py
      - run: python -m py_compile sethiportfolio.py
      - name: Commit patched backend
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sethiportfolio.py
          git diff --cached --quiet && exit 0
          git commit -m "Use BoE source for historical transaction FX [skip ci]"
          git push
