name: Patch allocation correction public reason
on:
  push:
    branches: [feature/sethiportfolio-allocation-correction]
    paths:
      - scripts/patch_allocation_correction_public_reason.py
      - .github/workflows/patch-allocation-correction-reason.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/sethiportfolio-allocation-correction
      - run: python scripts/patch_allocation_correction_public_reason.py
      - run: python -m py_compile sethiportfolio.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sethiportfolio.py
          git diff --cached --quiet && exit 0
          git commit -m "Preserve public rationale across allocation corrections [skip ci]"
          git push origin HEAD:feature/sethiportfolio-allocation-correction
