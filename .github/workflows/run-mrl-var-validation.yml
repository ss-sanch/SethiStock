name: Run MRL VaR validation migration
on:
  push:
    branches:
      - feature/mrl-var-validation
    paths:
      - scripts/apply_mrl_var_validation.py
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/mrl-var-validation
      - run: python scripts/apply_mrl_var_validation.py
      - run: python -m py_compile market_risk_lab.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add market_risk_lab.py
          git diff --cached --quiet && exit 0
          git commit -m "Add rolling VaR model validation [skip ci]"
          git push origin HEAD:feature/mrl-var-validation
